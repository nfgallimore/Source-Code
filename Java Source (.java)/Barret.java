if (x > m * Math.pow(b, 1-k)
	x = x-m * Math.pow(b, 1-k)

for ( int i = L-1; i > k-1; i--)
	do {
		if ( x_i == m_(k-1) )
			q = b - 1;
		else
			q = (x_i) * b + x_(i-1)
		while (q * (m_(k-1) * b + m_(k-2) > x_i * Math.pow(b, 2) + x_(i-1) * b + x_(i-2)))
	}